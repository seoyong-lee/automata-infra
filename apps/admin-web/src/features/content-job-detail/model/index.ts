export type CompareRow = {
  label: string;
  focus: string;
  hook: string;
  renderer: string;
  score: number;
};

export type SeedForm = {
  contentId: string;
  targetLanguage: string;
  titleIdea: string;
  targetDurationSec: string;
  stylePreset: string;
};
